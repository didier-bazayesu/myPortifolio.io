
// Placeholder file kept so existing imports won't break if reintroduced.
// Gemini / AI functionality has been intentionally removed to keep this
// project as a standard portfolio without external AI features.

export async function askDidierAI(_userQuery: string): Promise<string> {
  return "AI assistant has been disabled in this portfolio version.";
}
